<div
  class="absolute left-0 top-0 h-full w-full select-none overflow-hidden"
  data-scroll
  data-scroll-speed="-.2"
>
  <div class="bg-hero-image">
    <!-- Image overlays -->
    <div
      class="absolute left-0 top-0 h-full w-full select-none bg-blue-400 opacity-10"
    >
    </div>
    <div
      class="absolute left-0 top-0 h-full w-full select-none bg-white opacity-30 md:opacity-10"
    >
    </div>
  </div>
</div>

<!-- Position hero image on the page and apply responsive src loading -->
<style lang="sass">
  // Image src
  $image--portrait: "/img/main-hero__portrait-min.webp"

  $image: "/img/main-hero" // public folder

  // Portrait mode (for mobile devices)
  .bg-hero-image
    background-size: cover
    width: 100%
    height: 100%
    background-position: center bottom
    background-image: url("#{$image--portrait}?w=768")
    will-change: transform

  @media screen and (-webkit-min-device-pixel-ratio: 2)
    .bg-hero-image
      background-image: url("#{$image--portrait}?w=1536")

  // Landscape mode
  @media (orientation: landscape) and (min-width: 768px)
    .bg-hero-image
      background-position: center center
      width: 100%
      height: 100%
      background-image: url("#{$image}_small.jpg")

  @media (orientation: landscape) and (min-width: 768px) and (-webkit-min-device-pixel-ratio: 2), (orientation: landscape) and (min-width: 1280px)
    .bg-hero-image
      background-image: url("#{$image}_medium.jpg")

  @media (orientation: landscape) and (min-width: 1280px) and (-webkit-min-device-pixel-ratio: 2), (min-width: 1500px)
    .bg-hero-image
      background-image: url("#{$image}_large.jpg")
</style>